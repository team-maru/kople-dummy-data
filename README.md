# KOPLE Dummy Data Import Guide

이 디렉토리는 **백엔드 기능 테스트 및 화면 확인용 더미데이터**를
MySQL(RDS/Local)에 안전하게 적재하기 위한 자료입니다.

---

## 1. 목적 (Purpose)

- API 기능 테스트
- 게시글 / 댓글 / 좋아요 / 이미지 조회 화면 확인
- 페이징, 정렬, 집계 컬럼 검증
- 실제 서비스와 유사한 데이터 분포 확인

> ⚠️ 성능 테스트용 대용량 데이터는 아닙니다.

---

## 2. 데이터 구성 (Data Overview)

### 👤 Member / Profile / Buddy
- 회원 수: **50명**
- 모든 회원:
  - 온보딩 완료 상태 (`is_registered = true`)
  - 정지 아님 (`is_suspended = false`)
  - Buddy 카드 생성됨
  - Buddy `is_active = true`
- 닉네임:
  - 국적에 맞는 실제 사람 이름 형태
- 학교:
  - 다양한 국내 대학 코드 랜덤 분포
- 관심사:
  - 1인당 2~5개 (중복 없음)

---

### 📝 Free Post
- 게시글 수: **50개**
- 카테고리:
  - CAMPUS / DAILY_TIPS / EATS / ACTIVITY / QNA
- 제목 / 본문:
  - 영어
  - 실제 글로벌 캠퍼스 커뮤니티 톤
- 작성자:
  - 50명 회원 중 랜덤

---

### 💬 Comment
- 댓글 수: **약 80개**
- 구조:
  - **대댓글 없음**
  - 모든 댓글은 게시글에 직접 달림
- 목적:
  - 댓글 리스트, 카운트, 좋아요 테스트 안정성 확보

---

### ❤️ Likes
- 게시글 좋아요:
  - 게시글당 약 3~7개
- 댓글 좋아요:
  - 일부 댓글에 랜덤 분포
- 좋아요 수는 import 후 집계 쿼리로 계산됨

---

### 🖼 Image
- 이미지 대상:
  - Free Post
- 이미지 개수:
  - 약 20개 게시글에 1장씩
- 저장 방식:
  - **S3 key만 DB에 저장**
  - 예: `free/post_12.jpg`

> ⚠️ 더미 이미지는 S3에 미리 업로드되어 있어야 합니다.
> (삭제/수정 API 테스트 시 S3_DELETE_FAIL 방지 목적)

---

## 3. 파일 목록

```text
member.csv
profile.csv
buddy.csv
member_interest.csv
free_post.csv
comment.csv
free_like.csv
comment_like.csv
image.csv
import.sql
```

---

## 4. Import 실행 방법

### 1️⃣ MySQL 설정 확인

```sql
SHOW VARIABLES LIKE 'local_infile';

```

값이 `ON` 이어야 합니다.

---

### 2️⃣ import.sql 실행

```bash
mysql -u {USER} -p --local-infile=1 {DB_NAME} < import.sql

```

또는

- MySQL Workbench
- DBeaver
    
    에서 `import.sql` 전체 실행
    

---

## 5. Import 순서 (중요)

`import.sql` 내부에서 아래 순서로 처리됩니다:

1. member
2. profile
3. buddy
4. member_interest
5. free_post
6. comment
7. free_like
8. comment_like
9. image
10. 집계 컬럼 업데이트
    - `free_post.like_count`
    - `free_post.comment_count`
    - `comment.like_count`

---

## 6. 주의사항 (Important Notes)

- 이 더미데이터는 **조회/화면 확인용**입니다.
- 댓글은 **대댓글 구조를 포함하지 않습니다.**
- 더미 이미지 URL은 실제 S3 객체를 가리킵니다.
- 더미 환경에서는 이미지 수정/삭제 API 사용을 권장하지 않습니다.

---
